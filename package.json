{
  "name": "agora402",
  "private": true,
  "description": "The trust layer for agent-to-agent commerce. PayPal for autonomous agents.",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  }
}
